## Summary

<!-- What this PR does and why. Keep this updated as the discussion evolves. -->

## Test plan

- [ ] `make analyze`
- [ ] `make testing` (if tests were added/updated)
- [ ] Manual QA of the changed flow (if UI)

## Checklist

- [ ] Export new public files to the matching barrel (`domain.dart` / `data.dart` / `ui.dart` / `shared.dart` / `resources.dart`)
- [ ] Import via barrels only
- [ ] `Dimens` values use `.responsive()`
- [ ] No leftover commented-out code
- [ ] Tests created/updated for new logic
- [ ] Unused code and dependencies removed
- [ ] User-facing strings added to ARB + `make l10n`

## Screenshots

_Required for UI changes._

| Before | After |
| ------ | ----- |
|        |       |
